export type IExperience = {
    id: string;
    company: string;
    position: string;
}

export type IArticles = {
    title: string;
    url: string;
    id: string;
};

export type ICertificates = {
    blockchainId: string;
    title: string;
}

export type IRepositories = {
    name: string;
    description: string;
    url: string;
}

export type TNameObject = {
    name: string;
};
